#!/bin/bash
TARGET="$*"
if [[ "$TARGET" =~ ^[0-9]+$ ]] ; then
  TARGET="10.18.3.$TARGET"
  echo $TARGET
fi 
echo $(basename $0)
[ $(basename $0) = vv ] && CMD="vncviewer Fullscreen ViewOnly=1 PasswordFile=~/vnc Log *:stdout:100 $TARGET"
[ $(basename $0) = vc ] && CMD="vncviewer Fullscreen PasswordFile=~/vnc Log *:stdout:100 $TARGET"
echo $CMD
eval $CMD
